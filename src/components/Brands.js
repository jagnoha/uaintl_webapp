import React, { useState } from 'react';

const divStyle = {
  margin: '3em',
  marginTop: '5em',
};
const Brands = (props) => {
  return (
    <div style={divStyle}>
      <h1>Brands</h1>
    </div>
  );
};

export default Brands;
