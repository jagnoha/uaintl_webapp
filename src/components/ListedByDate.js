import React, { useState } from 'react';

const ListedByDay = (props) => {
  return (
    <div style={props.style}>
      <h1>Products Listed by Date</h1>
    </div>
  );
};

export default ListedByDay;
