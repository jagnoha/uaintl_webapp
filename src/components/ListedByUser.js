import React, { useState } from 'react';

const ListedByUser = (props) => {
  return (
    <div style={props.style}>
      <h1>Products Listed by User</h1>
    </div>
  );
};

export default ListedByUser;
