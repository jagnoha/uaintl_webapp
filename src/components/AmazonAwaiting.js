import React, { useState } from 'react';

const AmazonAwaiting = (props) => {
  return (
    <div style={props.style}>
      <h1>Amazon Orders (Awaiting Shipment)</h1>
    </div>
  );
};

export default AmazonAwaiting;
