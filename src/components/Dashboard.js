import React, { useState } from 'react';

const divStyle = {
  margin: '3em',
  marginTop: '5em',
};

const Dashboard = (props) => {
  return (
    <div style={divStyle}>
      <h1>Dashboard</h1>
    </div>
  );
};

export default Dashboard;
