import React, { useState } from 'react';

const AmazonAccounts = (props) => {
  return (
    <div style={props.style}>
      <h1>Amazon Accounts</h1>
    </div>
  );
};

export default AmazonAccounts;
