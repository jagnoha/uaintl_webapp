import React, { useState } from 'react';

const divStyle = {
  margin: '3em',
  marginTop: '5em',
};
const Locations = (props) => {
  return (
    <div style={divStyle}>
      <h1>Locations</h1>
    </div>
  );
};

export default Locations;
